# ABROJ Field Inspection Backend (Ready for Render)
1. Upload all these files to your GitHub repo (Select all → Upload files).
2. In Render: 
   - Build Command: npm install
   - Start Command: node server/app.js
   - Add environment variables from .env.example
